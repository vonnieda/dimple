import { Palette } from "std-widgets.slint";
export component HorizontalRule inherits Rectangle {
    height: 1px;
    background: Palette.border;
}

export component VerticalRule inherits Rectangle {
    width: 1px;
    background: Palette.border;
}

