import { Button , LineEdit, HorizontalBox, GroupBox} from "std-widgets.slint";
import { Navigator } from "common.slint";

export component NavBar inherits HorizontalBox {
    padding: 0px;

    // Button {
    //     text: "Home";
    //     clicked => { Navigator.navigate("dimple://home"); }
    //     primary: true;
    // }
    Button {
        text: "Artists";
        clicked => { Navigator.navigate("dimple://artists"); }
    }
    Button {
        text: "Releases";
        clicked => { Navigator.navigate("dimple://releases"); }
    }
    Button {
        text: "Genres";
        clicked => { Navigator.navigate("dimple://genres"); }
    }
    LineEdit {
        horizontal-stretch: 1;
        placeholder-text: "Search for artists, albums, songs, genres, lyrics, bios, years, instruments, etc.";
        // TODO change back to edited when better handling in the search function
        accepted(query) => { Navigator.navigate("dimple://search/" + query); }
    }
}
