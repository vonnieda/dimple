import { HorizontalBox, LineEdit, Palette, Button } from "std-widgets.slint";
import { Styles, Navigator, AppState } from "./common.slint";
import { Texxt } from "common.slint";

component NavBarButton inherits TouchArea {
    in property <image> source;
    width: image.width;
    image := Image {
        source: root.source;
        colorize: parent.has-hover 
            ? Palette.accent-foreground
            : Palette.foreground;
        width: 24px;
        height: 24px;
    }
}

export component NavBar inherits HorizontalBox {
    padding: 0;
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/sidebar-simple.svg");
        clicked => { AppState.sidebar_open = !AppState.sidebar_open; }
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/queue.svg");
        clicked => { Navigator.navigate("dimple://queue") }
    }
    Rectangle {
        width: parent.spacing;
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/arrow-left.svg");
        clicked => { Navigator.navigate("dimple://back") }
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/arrow-right.svg");
        clicked => { Navigator.navigate("dimple://forward") }
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/arrow-clockwise.svg");
        clicked => { Navigator.navigate("dimple://refresh") }
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/house.svg");
        clicked => { Navigator.navigate("dimple://home") }
    }
    LineEdit {
        placeholder-text: "Search for songs, artists, genres, playlists, URLs...";
        edited(query) => { Navigator.navigate("dimple://search/" + query); }
        accepted(query) => { Navigator.navigate("dimple://search/" + query); }
        font-size: Styles.default-font-size;
        height: self.font-size * 2;
    }
    NavBarButton {
        source: @image-url("../icons/phosphor/SVGs/regular/gear.svg");
        clicked => { Navigator.navigate("dimple://settings") }
    }

    // Button {
    //     width: self.min-width;
    //     clicked => {
    //         if Styles.default-font-family == "Commissioner" {
    //             Styles.default-font-family = "Inter";
    //         }
    //         else if Styles.default-font-family == "Inter" {
    //             Styles.default-font-family = "Manrope";
    //         }
    //         else if Styles.default-font-family == "Manrope" {
    //             Styles.default-font-family = "Noto Sans";
    //         }
    //         else if Styles.default-font-family == "Noto Sans" {
    //             Styles.default-font-family = "Outfit";
    //         }
    //         else if Styles.default-font-family == "Outfit" {
    //             Styles.default-font-family = "Roboto";
    //         }
    //         else if Styles.default-font-family == "Roboto" {
    //             Styles.default-font-family = "Source Code Pro";
    //         }
    //         else if Styles.default-font-family == "Source Code Pro" {
    //             Styles.default-font-family = "Source Sans 3";
    //         }
    //         else if Styles.default-font-family == "Source Sans 3" {
    //             Styles.default-font-family = "Wingdings";
    //         }
    //         else if Styles.default-font-family == "Wingdings" {
    //             Styles.default-font-family = "Inter";
    //         }
    //     }
    //     text: "Font";
    // }
    // Texxt {
    //     text: Styles.default-font-family;
    // }
    // Button {
    //     width: self.min-width;
    //     clicked => {
    //         Styles.default-font-size -= 0.5px;            
    //     }
    //     text: "-";
    // }
    // Button {
    //     width: self.min-width;
    //     clicked => {
    //         Styles.default-font-size += 0.5px;            
    //     }
    //     text: "+";
    // }
    // Texxt {
    //     text: Styles.default-font-size / 1px;
    // }

    // Button {
    //     width: self.min-width;
    //     clicked => {
    //         Styles.default-font-weight -= 25;            
    //     }
    //     text: "-";
    // }
    // Button {
    //     width: self.min-width;
    //     clicked => {
    //         Styles.default-font-weight += 25;            
    //     }
    //     text: "+";
    // }
    // Texxt {
    //     text: Styles.default-font-weight;
    // }
    // Texxt {
    //     text: "test";
    //     font-weight: 100;
    // }
    // Texxt {
    //     text: "test";
    //     font-weight: 400;
    // }
    // Texxt {
    //     text: "test";
    //     font-weight: 700;
    // }
    // Texxt {
    //     text: "test";
    //     font-italic: true;
    // }

}
