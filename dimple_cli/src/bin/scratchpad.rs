// use std::sync::{Arc, Mutex};

// use playback_rs::Player;



// fn main() {
//     let player = Player::new(None).expect("player");
//     std::thread::spawn(move || {
//         // player.lock().unwrap().set_playing(true);
//     });
// }